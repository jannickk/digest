variables:
  storage_account_name:
    description: The storage account name where to look 
  container:
    description: The container in which new files are going to be deposited
  path_to_monitor:
    description: The path within the container to monitor for new files
  existing_cluster_id:
   description: The id of the cluster on hwich to run the jobs
  catalog:
    description: The catalog where to deploy the resources
  schema:
    description: The schema into which to deploy create tables